import 'package:flutter/material.dart';

class PaymentsTab extends StatelessWidget {@override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Payments", textAlign: TextAlign.center),
    );
  }
}
